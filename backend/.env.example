# Backend Configuration
PORT=3001

# JWT Secret (Change this to a random secure string)
JWT_SECRET=super-secret-multi-whatsapp-key-123

# Database
DB_NAME=sessions.db

# CORS (Frontend URL)
FRONTEND_URL=https://m53fnxg1-5173.asse.devtunnels.ms/

# Optional: Default AI Configuration
# Users will add their own keys in the UI, but you can set defaults here
DEFAULT_AI_PROVIDER=none
DEFAULT_AI_MODEL=gpt-3.5-turbo
